#!/bin/sh

# Initialize our own variables:
mx=0

OPTIND=1
while getopts "h?m" opt; do
    case "$opt" in
        h|\?)
            usage
            exit 0
            ;;
        m)  mx=1
            ;;
    esac
done
shift $((OPTIND-1))

echo "mx=$mx, Leftovers: $@"

# End of file